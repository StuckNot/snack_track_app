import 'package:flutter/material.dart';

class AuthPadding{

  static const EdgeInsets page = EdgeInsets.all(20);

}

class AuthSpacing{
  static const double standard = 20;
}

class AuthBorder{
  static  BorderRadius border = BorderRadius.only(topRight:Radius.circular(20),topLeft: Radius.circular(20),);
}